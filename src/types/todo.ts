export interface Todo {
  id: number;
  task: string;
  description: string;
  category: string;
  dueDate: Date;
  priority: string;
  completed: boolean;
  createdAt: string;
  updatedAt: string;
}
