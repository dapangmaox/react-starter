export interface Todo {
  id: number;
  task: string;
  description: string;
  category: string;
  dueDate: string;
  priority: string;
  completed: boolean;
  createdAt: string;
  updatedAt: string;
}
